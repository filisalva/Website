document.getElementById('registerForm').addEventListener('submit', function(e) {
    e.preventDefault();
    // Codice per inviare la richiesta di registrazione al server
});

document.getElementById('loginForm').addEventListener('submit', function(e) {
    e.preventDefault();
    // Codice per inviare la richiesta di login al server
});

document.getElementById('filterForm').addEventListener('submit', function(e) {
    e.preventDefault();
    // Codice per filtrare e visualizzare la classifica
});

document.getElementById('addMatchForm').addEventListener('submit', function(e) {
    e.preventDefault();
    // Codice per aggiungere una partita al database
});

document.getElementById('addPlayerForm').addEventListener('submit', function(e) {
    e.preventDefault();
    // Codice per aggiungere un giocatore al database
});